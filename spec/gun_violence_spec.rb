require 'spec_helper'

describe GunViolence do
  it 'has a version number' do
    expect(GunViolence::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
